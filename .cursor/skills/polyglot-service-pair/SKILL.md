---
name: polyglot-service-pair
description: >-
  Build a two-component polyglot system — FastAPI service plus Node CLI client.
  Currency conversion demo with validation, tests, README, and verification output.
  Use when the user types /polyglot-service-pair or asks for FastAPI + Node CLI
  paired implementation.
disable-model-invocation: true
---

# Polyglot Service Pair (slash command entry)

Read and follow **`Intermediate-repo operator and polyglot builder/I4/agent.md`** in full — that file is the source of truth for workflow, rules, and deliverables.

The reference implementation is in **`Intermediate-repo operator and polyglot builder/I4/`** with:

- `services/fastapi/` — conversion API
- `clients/node-cli/` — Node CLI
- `README.md` — architecture and run instructions
- `validation-results.md` — verification output

When building or extending, keep all documentation in `.md` files within `I4/` (no separate `.mmd` unless requested).
